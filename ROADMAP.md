# ROADMAP — process spine

> The wiki owns *what's true*; this file owns *when and in what order*. It references the
> canonical registers (`REQ-*`, `D-*`, `Q-*`) and never becomes a second editable home for
> them. Specs are indexed here and live in `specs/`.

## Phases

| Phase | Goal | Status | Specs |
| --- | --- | --- | --- |
| 0 — Ingest | All current sources folded in; registers populated | ☐ | — |
| 1 — `<first slice>` | `<goal>` | ☐ | `specs/<feature>.md` |
| 2 — `<next slice>` | `<goal>` | ☐ | — |

## Now / Next / Later

- **Now:** `<the single active step>`
- **Next:** `<queued>`
- **Later:** `<parked, with why>`

## Log

- `<date>` — roadmap created.
