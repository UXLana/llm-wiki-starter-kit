# Specs index

Per-feature build contracts. A spec references the canonical registers (`REQ-*`, `D-*`,
`Q-*`); it never re-authors them. Indexed in `ROADMAP.md`.

| Spec | Delivers | Status |
| --- | --- | --- |
| `_TEMPLATE.md` | — | template |
